import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/TopForm';

//take component's html and render onto page in the DOM
ReactDOM.render(
    <App />, document.querySelector('.container'));
